---
description: 代码质量规范 - TypeScript、异步处理、错误处理与注释
alwaysApply: true
---

# 代码质量规范

## TypeScript 强制规范

- **所有新代码必须使用 TypeScript**，禁止新建 `.js` / `.jsx` 文件
- 禁止使用 `any`，用 `unknown` 替代后做类型收窄，或定义明确的接口/类型
- 函数参数与返回值必须有明确的类型标注

```typescript
// ❌ 使用 any
function process(data: any) { return data.value; }

// ✅ 明确类型
interface UserData { value: string; id: number; }
function process(data: UserData): string { return data.value; }
```

## 异步处理：async/await 优先

**优先使用 async/await，禁止在新代码中使用回调嵌套（callback hell）**：

```typescript
// ❌ 回调风格
fetchUser(id, (err, user) => {
  if (err) return handleError(err);
  fetchOrders(user.id, (err, orders) => { /* ... */ });
});

// ✅ async/await 风格
const user = await fetchUser(id);
const orders = await fetchOrders(user.id);
```

## 错误处理与日志记录

异步操作和可能抛出错误的代码必须包含完整的 try/catch 和日志记录：

```typescript
// ❌ 错误示范
async function fetchUserData(id: string) {
  const res = await fetch(`/api/users/${id}`);
  return res.json();
}

// ✅ 正确示范
async function fetchUserData(id: string) {
  try {
    const res = await fetch(`/api/users/${id}`);
    if (!res.ok) throw new Error(`HTTP ${res.status}`);
    return await res.json();
  } catch (error) {
    console.error('[fetchUserData] 获取用户数据失败', { id, error });
    throw error;
  }
}
```

## 代码注释规范

- 复杂逻辑、非显而易见的实现必须添加注释说明**意图**，而非重复代码本身
- 不要写"// 定义变量"、"// 调用函数"等无意义注释
- 业务逻辑关键步骤用注释标注背景或原因
